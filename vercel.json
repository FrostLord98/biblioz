{
  "builds": [
    { "src": "app.py", 
      "use": "@vercel/python-wsgi",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" }
  ]
}
